# Fab

Mobile-only floating action button that opens capture. Desktop uses the
sidebar's "What's on your mind?" instead.

Hidden when the settings menu is open (checked here) and when capture is open
(checked in `AppShell`).

Positioned above the tab bar: `bottom: tabbar-h + 1.125rem + safe-area`.
